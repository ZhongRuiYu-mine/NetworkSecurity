# 评测协议 v1（Evaluation Protocol）

> 本文件是四个方向合并后**唯一的评测口径来源**。
> 任何写进报告/论文的数字，都必须按本协议产出；跨口径比较一律视为无效。
>
> 制定依据：`docs/INTERFACE.md`（契约）、`AUTO.pdf`（AE+IF 融合论文）、
> `Autoencoder-Based_Anomaly_Detection.pdf`（GOOSE 论文），以及根目录
> `GAP_ANALYSIS.md` 中实测出的两个协议缺陷。

---

## 0. 为什么需要这份协议（实测证据）

合并前，同一份 CICIDS2017、同一批检测器，在不同目录里用**不同测试集 + 不同阈值口径**
算出数字，然后被放进同一张对比表。两个实测缺陷：

| 缺陷 | 实测 | 影响 |
|---|---|---|
| **自评污染** | 平台测试集 29000 行中 **4066 行（14.02%）与 `X_normal_train` 逐位重复** | 检测器在自己拟合过的样本上被评估 |
| **阈值口径不一致** | 平台传 `y_test` 进 `fit()` → 走 F1 扫描；`ae_repro` 走良性分位数 | 同一个 IF：F1 **0.591 vs 0.465**（α=0.05）vs **0.297**（α=0.01） |

复算脚本：`_analysis/leak_impact.py`（见 `GAP_ANALYSIS.md` §7）。
复算结论：自评污染本身对 IF 影响小（F1 0.591→0.611，FPR 不变），
**真正致命的是阈值口径**——所以本协议对两者都做了硬约束。

---

## 1. 数据协议

| 项 | 规定值 | 理由 |
|---|---|---|
| 数据集 | CICIDS2017（`chethuhn` 原始 CSV，78 维） | 与平台 `data_loader.py`、`taxonomy.py` 对齐 |
| 规范数据包 | `data/cicids_sample.npz` | 唯一评估数据来源；由 `python -m ae_repro.data_prep` 生成 |
| 良性/检测器拟合集 | `X_normal_train`，60000 行（仅 Monday 良性） | 检测器**只能**用它 fit；`AUTO.pdf` Eq.1 的归一化统计量也只来自它 |
| 带标签训练集 | `X_train` (36875, 78)，良性下采样至 30% | MADDPG 交互用 |
| **评估集** | `X_test` (25000, 78)，**已排除 Monday 行** | 协议 v1 的核心修复：Monday 是检测器的拟合数据，不能进评估集 |
| 归一化 | Min-Max，统计量**仅**来自 `X_normal_train` → `[0,1]` | 论文 Eq.1 |
| 采样 | 每文件 ≤20000 行（流式分块），Monday 良性 60000 行 | 与平台默认一致，便于横向对比 |
| 类别空间 | `BENIGN / DDoS / PortScan / DoS / WebAttack / Other`（6 类） | `class_preset=default`；样本 <200 的攻击族并入 `Other` |
| 随机种子 | `seed=42` | 切分、采样、下采样统一 |

**硬规则**

1. 检测器的 `fit()` **只接收良性流量**。任何把攻击样本喂进拟合流程的做法都视为错误。
2. 评估集**不得**包含用于拟合检测器的那批行。数据包更新后必须重新核对
   `X_test ∩ X_normal_train` 的重复率，**目标 ≤1%**（协议 v1 实测 0.70%）。
3. 换特征子集 / 换类别空间 = 换协议，必须重新生成数据包并重新 fit 所有检测器，
   且已在报告中声明的数字全部作废。

---

## 2. 阈值口径协议（★ 最容易出错的地方）

平台 `INTERFACE.md` §2.8 允许两种口径，本协议**强制区分主口径与辅口径**：

| 口径 | 怎么定阈值 | 用途 | 是否可引用 |
|---|---|---|---|
| **主口径：良性分位数** `alpha=0.05` | 取 `X_normal_train` 上分数的 95% 分位 | 上线/无标签场景；**报告的主数字** | ✅ 可引用，且可外推到真实基率 |
| 辅口径：F1 扫描 | 在评估集标签上扫 F1 最大阈值 | 复现论文的乐观上界 | ⚠️ 只能作为"上界"并列报告，**必须标注"用了测试标签"** |

**硬规则**

1. 评估脚本调用 `detector.fit(X_normal)` 时**不传** `X_test/y_test`
   （传了就会触发平台的 F1 扫描，等于用测试标签选阈值）。
2. 报告里每个数字后面必须写清用的是哪个口径。**禁止**把主口径与辅口径的数字
   放进同一列/同一张表做比较。
3. 三种以上机制的对比，口径必须完全一致（都用主口径，或都用辅口径）。
4. Kalman 的 `mode`（`frozen`/`decay`/`forget`）必须随结果一起记录；
   三方横向对比建议 `mode="frozen"`（无时序记忆，与 IF/AE 最公平）。

---

## 3. 维度与可复现性协议

| 项 | 规定值 | 理由 |
|---|---|---|
| `embed_dim` | **4**（所有机制统一） | `INTERFACE.md` §2.4：维度一致才能"只换检测器、其它全不动" |
| `signal_dim` | `3 + 4 = 7` | 观测尾部信号向量 |
| `feat_dim` | 78 | 数据包特征维 |
| `num_classes` | 6 | 含 BENIGN |
| `include_focus_onehot` | `True` | 与平台默认一致 |
| `seed` | 42（数据）/ 0（MADDPG） | 与各包原值一致，不额外调参 |

---

## 4. 评估指标协议

**每个检测器/机制都要报全套**，不允许只挑好看的：

| 指标 | 定义 | 备注 |
|---|---|---|
| Precision / Recall / F1 | 二分类（攻击 vs 良性） | 主口径阈值下的工作点 |
| ROC-AUC | 与阈值无关的排序能力 | 用于跨容量比较 |
| PR-AUC | 同上 | 类不均衡下比 AUC 更有信息量 |
| FPR | `FP/(FP+TN)` | **只在良性样本上算，与攻击占比无关，可直接外推** |
| **FP‰** | 每千条流量的误报数 | 提案第 4 节要求 |
| **Precision@1% / @0.1%** | 按真实基率校正后的精度 | 提案第 4 节要求；**报告 P 时必须同时给这个** |
| 按攻击类别的检出率 | 每类 recall | `AUTO.pdf` 口径；必须暴露弱项（如 PortScan/WebAttack ≈0） |

**硬规则**

- 报告 precision 时必须注明评估集的攻击占比；评估集是**分层抽样**（v1：攻击 25.8%），
  远高于线上基率，直接引用 P 会高估可部署性。
- 评估子集必须分层抽样（`utils.metrics.stratified_subset`），否则会取到全是 BENIGN。

---

## 5. 结果产物规范

| 产物 | 位置 | 内容 |
|---|---|---|
| 检测器对比表 | `results/detector_comparison.md` | ★ 四种机制同口径主表（替换旧 docx 表 5.1） |
| 机器可读结果 | `results/detector_comparison.json` | 上表的原始数字，含全部口径与配置 |
| 各机制单测结果 | `results/<detector>_only/` | 每个检测器的 ROC 图、分数分布、落盘检测器 |
| 策略训练产物 | `results/<detector>/` | MADDPG 权重 / history / eval |

生成方式：`python scripts/eval_all_detectors.py`（一条命令产出上表）。

---

## 6. 尚未裁决的议题（必须由组内书面决定后才写入论文）

| # | 议题 | 现状 | 建议 |
|---|---|---|---|
| 1 | **带符号 embedding vs `normalize_obs` 裁剪** | `INTERFACE.md` §2.2 要求 embedding 放"偏离方向"（带符号），§3.3 又把整行观测裁到 `[0,1]` → 负号被吃掉，契约自相矛盾 | 二选一：① 观测只对 `[0:feat_dim]` 做裁剪，embedding 段用 `tanh` 压缩；② 检测器输出改为非负有向映射。**当前未改代码，默认行为仍是裁剪** |
| 2 | 端到端对比用哪个 AE | 平台内置 `ae`（64-32/latent 8/原始 MSE/embed 78）与 `ae_paper`（32-16-8/latent 4/逐特征标准化残差/embed 4）是两个模型 | 检测器层面两个都报；**端到端 MADDPG 对比建议用 `ae_paper`**（与 IF/Kalman 口径一致） |
| 3 | 是否补跑 Kalman / AE / null 的策略训练 | 目前只有 IF 有策略权重 | 必须补，否则"只换检测器"的核心结论没有证据 |
| 4 | ICPS 融合层如何进平台 | `network` 包 0 处实现 `DetectorBase`；物理量测与 78 维流特征语义不同 | 需要专门设计（融合层输出 3+k 维信号），不在协议 v1 范围内 |
| 5 | 10 折交叉验证 | `AUTO.pdf` 用 10 折，本项目用跨天单次划分 | 保持跨天划分（更接近真实），但报告中必须写明"未做 10 折"及其理由 |

---

## 7. 变更记录

| 版本 | 变更 | 依据 |
|---|---|---|
| v1 | 建立统一数据包（评估集排除 Monday）；强制区分主/辅阈值口径；统一 `embed_dim=4`；`ae_paper` 正式注册进 `agentenvs.detectors` | `GAP_ANALYSIS.md` §2.1、§2.2、§3 |
