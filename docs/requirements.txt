# MADDPG 多智能体网络防御平台 —— 依赖
#
# 目标环境：D:\PycharmProjects\pythonProject\.venv （Python 3.12）
# 安装： & $PY -m pip install -r requirements.txt
#
# 实测可用版本（venv 里已装的版本）
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
torch>=2.0
matplotlib>=3.7
joblib>=1.3

# 可选：Gym 风格外壳（没有也能跑，环境会退化成一个纯 Python 类）
# gymnasium>=0.29
# gym>=0.26
